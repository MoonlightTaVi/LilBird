get:
    summary: Returns a simple list of all bird entries.
    description: The list contains only ID and the main latin & common names.
    responses:
      "200":
        description: A JSON array of all bird objects.
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '../schemas/birds-list.yml'
